# Streamlit部署专用依赖文件 - 使用ultralytics-opencv-headless避免GUI依赖
streamlit>=1.32.0
ultralytics-opencv-headless>=8.4.46  # 无GUI版本，适合服务器环境（必须使用headless版本）
pillow>=10.0.0
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
pandas>=2.0.0  # 用于CSV文件读取
# 可选：如果需要使用GPU，保持torch版本与CUDA兼容
# 注意：Streamlit Cloud通常只提供CPU环境